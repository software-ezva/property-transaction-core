{
  "default": {
    "formatOptions": {
      "snippetInterface": "synchronous"
    },
    "require": [
      "features/step_definitions/**/*.ts",
      "features/support/**/*.ts"
    ],
    "requireModule": ["ts-node/register"],
    "format": ["progress-bar"]
  }
}
